# MEMORY — ai.toolkit.gaia

- [Current state](memory/state.md) — **v13.0.0 UNCOMMITTED**: all 15 tasks/memory/evolve tools removed, the `fa.integrations` Woolworths capability migrated in whole (4 MCP tools + REST `/api`), 9 plugin files retargeted onto native todos and the file-based memory store, first real test suite (12/12); origin serves 12.1.0 and the hosted service still serves the old tools
- [Live decisions](memory/decisions.md) — 13.0.0: tools removed not deprecated (contract now unenforced, deliberately), `foundation` wires no MCP server, `fa.integrations` migrated in and killed, four projects folded into one, both surfaces kept, stdio credentials derived from headers
- [Gotchas](memory/gotchas.md) — `MSBuildEnableWorkloadResolver=false` mandatory here; **session mode silently governs whether header credentials resolve** (verified, re-verify if changed); upstream returns HTTP 200 for every failure; `dotnet test` is finally real, so pre-13.0.0 green means nothing
- [Open questions](memory/questions.md) — **⚠ BLOCKING BEFORE PUSH: this migration moves the Woolworths reverse-engineering from a private repo into this public one — an undecided call a push would make by default**; one repo now ships two unrelated products; placement reads as Technologies; main→tags
- [Watch list](memory/watch.md) — 13.0.0 uncommitted and undeployed; the v13 commit deletes 14 files **on purpose**; the `frostaura` marketplace was uninstalled from this machine and no plugin had been enabled anyway; `context-audit.py` still blind to plugin skills
