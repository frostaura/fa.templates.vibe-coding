# MEMORY — ai.toolkit.gaia

- [Current state](memory/state.md) — **v13.0.0 SHIPPED to `origin/main` 2026-08-21**: all 15 tasks/memory/evolve tools removed, the `fa.integrations` Woolworths capability migrated in whole (4 MCP tools + REST `/api`), 9 plugin files retargeted onto native todos and the file-based memory store, first real test suite (12/12). Live to every installed user; **the hosted MCP is not yet redeployed**
- [Live decisions](memory/decisions.md) — 13.0.0: tools removed not deprecated (contract now unenforced, deliberately), `foundation` wires no MCP server, `fa.integrations` migrated in and killed, four projects folded into one, both surfaces kept, stdio credentials derived from headers
- [Gotchas](memory/gotchas.md) — `MSBuildEnableWorkloadResolver=false` mandatory here; **session mode silently governs whether header credentials resolve** (verified, re-verify if changed); upstream returns HTTP 200 for every failure; `dotnet test` is finally real, so pre-13.0.0 green means nothing
- [Open questions](memory/questions.md) — the Woolworths reverse-engineering is now **published** (founder-authorised, exposure stated), so the live question is framing and a possible vendor conversation; one repo ships two unrelated products; placement reads as Technologies; main→tags
- [Watch list](memory/watch.md) — **plugins and hosted server now disagree** — users have 13.0.0, the live MCP still serves the old tools; v13 deleted 15 files **on purpose**; the `frostaura` marketplace was uninstalled from this machine and no plugin had ever been enabled; `context-audit.py` still blind to plugin skills
